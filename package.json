{
  "name": "mini-tools",
  "version": "0.0.0",
  "private": true,
  "description": "Da Excel a Software — monorepo di mini-tool desktop per PMI italiane",
  "packageManager": "pnpm@11.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && rm -rf node_modules",
    "gen:fixtures": "tsx scripts/gen-fixtures.ts",
    "gen:icons": "tsx scripts/gen-icons.ts"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.6.3",
    "@types/node": "^22.10.1",
    "@types/pngjs": "^6.0.5",
    "tsx": "^4.19.2",
    "vitest": "^2.1.8",
    "prettier": "^3.4.2",
    "pngjs": "^7.0.0",
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "unrs-resolver"
    ]
  }
}
